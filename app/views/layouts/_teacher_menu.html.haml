%ul.nav
  %li.dropdown
    = link_to "#{ name_for_school(current_user) }'s Curriculum Plan", "#", :class => "dropdown-toggle", :data => { :toggle => "dropdown" }
    %ul.dropdown-menu
      - for grade in current_user.school.grades
        %li= link_to School.grade_name_for(grade), show_curriculum_path(:grade => grade)
  %li= link_to "Reports", reports_path
  %li= link_to "Curriculum", public_curriculum_path
