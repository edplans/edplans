%ul.nav
  %li.dropdown
    = link_to "Teacher", '#', :class => 'dropdown-toggle', :data => { :toggle => 'dropdown' }
    %ul.dropdown-menu
      %li.dropdown-submenu
        = link_to "Curriculum Plan", show_curriculum_path
        %ul.dropdown-menu
          - for grade in current_user.school.grades
            %li= link_to School.grade_name_for(grade), show_curriculum_path(:grade => grade)
      %li= link_to "Show Curriculum", public_curriculum_path
      %li= link_to "Reports", reports_path
