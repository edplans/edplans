.row
  .span6.offset3
    = form_for @user, :url => me_path, :html => { :method => :put, :class => 'form-horizontal well' } do |f|

      %fieldset
        %legend= "Your User Settings"

        .control-group
          = f.label :first_name, "First Name", :class => 'control-label'
          .controls= f.text_field :first_name

        .control-group
          = f.label :first_name, "Last Name", :class => 'control-label'
          .controls= f.text_field :last_name

        .control-group
          = f.label :password, "Password", :class => 'control-label'
          .controls= f.password_field :password

        .control-group
          .controls= f.submit "Save", :class => 'btn btn-primary'